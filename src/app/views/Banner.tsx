import Image from "next/image";

const Banner = ({url}: any) => {
    return (<></>)
    // <Image src={`https://www.dashboard.hauscenter.com.bo/${url.attributes.imgBanner.data.attributes.url}`} width={1920} height={468} style={{width: '100%', height: 'auto'}} alt="Picture of the author" />
}

export default Banner;